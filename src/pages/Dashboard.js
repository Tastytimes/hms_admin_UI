import { Col, NavLink } from "react-bootstrap";
import Routing from "../Routing";
import {
  BrowserRouter as Router,
  Routes,
  Route,
  useRoutes,
  Link,
} from "react-router-dom";

const Dashboard = (props) => {
  return <h1>asjnjsn</h1>;
};

export default Dashboard;
